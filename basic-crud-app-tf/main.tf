terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
    random = {
      source  = "hashicorp/random"
      version = "~> 3.6"
    }
  }
}

provider "aws" {
  region = var.aws_region
}

data "aws_caller_identity" "current" {}

# Default VPC / subnets keep this demo simple - the EC2 instance sits in a
# public subnet (per requirement), the RDS instance sits in the same set of
# subnets but is not publicly accessible and is locked down by security group.
data "aws_vpc" "default" {
  default = true
}

data "aws_subnets" "default" {
  filter {
    name   = "vpc-id"
    values = [data.aws_vpc.default.id]
  }
}

# Always resolves to the latest Amazon Linux 2023 AMI - ships with the SSM
# Agent and AWS CLI v2 preinstalled, which is why it was chosen for this setup.
data "aws_ssm_parameter" "al2023_ami" {
  name = "/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64"
}
